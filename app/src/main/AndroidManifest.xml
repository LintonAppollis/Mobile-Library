<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devhub.mobi_library">

    <application
        android:allowBackup="true"
        android:icon="@drawable/library_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/library_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".edit_data"></activity>
        <activity android:name=".edit_books_activity" />
        <activity android:name=".view_books_activity" />
        <activity android:name=".delete_books_activity" />
        <activity android:name=".signup" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>